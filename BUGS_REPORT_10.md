# 🐛 Отчет о найденных багах (QA Analysis - Раунд 10)

**Дата анализа:** 9 декабря 2025  
**Аналитик:** QA Engineer  
**Методология:** Глубокий анализ API endpoints и валидации данных

---

## 🔴 КРИТИЧЕСКИЙ ПРИОРИТЕТ

### BUG-69: Отсутствие фильтрации по пользователю в `get_user_statistics`

**Статус:** ✅ **НЕ БАГ** - так задумано архитектурой системы

**Файл:** `orders/views/api_views.py:314-342`

**Описание:**
Функция возвращает статистику для всех заказов в системе, а не только для текущего пользователя. Это **не баг**, а функциональность системы - все пользователи работают с одними и теми же заказами, поэтому они должны видеть общую статистику.

**Примечание:** Исправление не требуется, так как это соответствует бизнес-требованиям системы.

---

### BUG-70: Дублирование docstring в `create_country_ajax`

**Файл:** `orders/views/api_views.py:97-113`

**Проблема:**
```python
def create_country_ajax(request):
    """
    Create a new country via AJAX.
    
    КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ BUG-50: Убрали @csrf_exempt, используем стандартную CSRF защиту Django.
    CSRF токен должен передаваться через заголовок X-CSRFToken в AJAX запросах.
    @ensure_csrf_cookie гарантирует установку CSRF cookie для проверки токена.
    """
    """
    Create a new country via AJAX.
    
    Request Body:
        JSON with country data (name, code)
    
    Returns:
        JsonResponse with success status and country data
    """
```

**Описание:**
Есть два docstring подряд. Первый docstring содержит комментарий об исправлении BUG-50, второй - описание функции. Это приводит к тому, что второй docstring перезаписывает первый, и информация об исправлении теряется.

**Исправление:**
Объединить docstring в один:
```python
def create_country_ajax(request):
    """
    Create a new country via AJAX.
    
    КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ BUG-50: Убрали @csrf_exempt, используем стандартную CSRF защиту Django.
    CSRF токен должен передаваться через заголовок X-CSRFToken в AJAX запросах.
    @ensure_csrf_cookie гарантирует установку CSRF cookie для проверки токена.
    
    Request Body:
        JSON with country data (name, code)
    
    Returns:
        JsonResponse with success status and country data
    """
```

---

## 🟠 ВЫСОКИЙ ПРИОРИТЕТ

### BUG-71: Отсутствие обработки null значений для `uploaded_at` в `get_factory_details`

**Файл:** `orders/views/api_views.py:437`

**Проблема:**
```python
'recent_orders': [
    {
        'id': order.id,
        'title': order.title,
        'status': order.status,
        'status_display': order.get_status_display(),
        'uploaded_at': order.uploaded_at.isoformat(),  # ⚠️ Может быть None!
    }
    for order in recent_orders
]
```

**Описание:**
Если `order.uploaded_at` равен `None`, вызов `.isoformat()` приведет к `AttributeError`. Хотя в модели `Order` поле `uploaded_at` может быть `null=True`, код не обрабатывает этот случай.

**Воспроизведение:**
1. Создать заказ без `uploaded_at` (если это возможно)
2. Вызвать API endpoint `/api/factory-details/<pk>/`
3. Получить `AttributeError: 'NoneType' object has no attribute 'isoformat'`

**Исправление:**
Добавить проверку на `None`:
```python
'uploaded_at': order.uploaded_at.isoformat() if order.uploaded_at else None,
```

---

### BUG-72: Отсутствие валидации длины полей в `create_country_ajax` и `create_factory_ajax`

**Файлы:** 
- `orders/views/api_views.py:120`
- `orders/views/api_views.py:196-198`

**Проблема:**
```python
# create_country_ajax
if not data.get('name') or not data.get('code'):
    return JsonResponse({
        'success': False,
        'message': 'Название и код страны обязательны'
    })
# ⚠️ НЕТ ПРОВЕРКИ ДЛИНЫ!

# create_factory_ajax
name = data.get('name', '').strip()
email = data.get('email', '').strip()
# ⚠️ НЕТ ПРОВЕРКИ ДЛИНЫ!
```

**Описание:**
Нет валидации максимальной длины для полей `name`, `code`, `email`, `contact_person`, `phone`, `address`. Это может привести к:
1. DoS атакам через очень длинные строки
2. Нарушению ограничений базы данных (если есть `max_length`)
3. Проблемам с отображением в UI

**Исправление:**
Добавить валидацию длины:
```python
# Для create_country_ajax
name = data.get('name', '').strip()
code = data.get('code', '').strip()

if not name or not code:
    return JsonResponse({
        'success': False,
        'message': 'Название и код страны обязательны'
    })

# КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ BUG-72: Валидация длины полей
if len(name) > 100:  # Согласно модели Country
    return JsonResponse({
        'success': False,
        'message': 'Название страны слишком длинное (максимум 100 символов)'
    })

if len(code) > 10:  # Согласно модели Country
    return JsonResponse({
        'success': False,
        'message': 'Код страны слишком длинный (максимум 10 символов)'
    })

# Для create_factory_ajax - аналогично для всех полей
```

---

## 🟡 СРЕДНИЙ ПРИОРИТЕТ

### BUG-73: Отсутствие валидации формата кода страны в `create_country_ajax`

**Файл:** `orders/views/api_views.py:120-124`

**Проблема:**
```python
if not data.get('name') or not data.get('code'):
    return JsonResponse({
        'success': False,
        'message': 'Название и код страны обязательны'
    })
# ⚠️ НЕТ ПРОВЕРКИ ФОРМАТА КОДА!
```

**Описание:**
Код страны обычно должен быть в формате ISO 3166-1 alpha-2 (2 символа) или alpha-3 (3 символа), но нет валидации формата. Можно создать страну с кодом "ABC123" или "123", что может привести к проблемам при интеграции с внешними системами.

**Исправление:**
Добавить валидацию формата:
```python
import re

code = data.get('code', '').strip().upper()

# КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ BUG-73: Валидация формата кода страны
# Код должен быть 2-3 буквы латиницы
if not re.match(r'^[A-Z]{2,3}$', code):
    return JsonResponse({
        'success': False,
        'message': 'Код страны должен состоять из 2-3 букв латиницы'
    })
```

---

### BUG-74: Отсутствие ограничения на количество результатов в `get_factories` и `get_countries`

**Файлы:**
- `orders/views/api_views.py:27-66`
- `orders/views/api_views.py:71-88`

**Проблема:**
```python
def get_factories(request):
    # ...
    factories = Factory.objects.filter(country_id=country_id).select_related('country')
    # ⚠️ НЕТ ОГРАНИЧЕНИЯ НА КОЛИЧЕСТВО РЕЗУЛЬТАТОВ!
    
    factories_data = [
        # ... преобразование в список
    ]
    
    return JsonResponse({'factories': factories_data})
```

**Описание:**
Если в системе тысячи фабрик или стран, API вернет все записи, что может привести к:
1. Большому размеру ответа (DoS)
2. Медленной работе API
3. Проблемам с памятью на клиенте

**Исправление:**
Добавить ограничение на количество результатов:
```python
from ..constants import ViewConstants

# КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ BUG-74: Ограничиваем количество результатов
factories = Factory.objects.filter(country_id=country_id).select_related('country')[:ViewConstants.MAX_PAGE_SIZE]
```

---

## 📊 Статистика Раунда 10

- **Критических багов:** 2 (фильтрация по пользователю, дублирование docstring)
- **Высокий приоритет:** 2 (обработка null, валидация длины)
- **Средний приоритет:** 2 (валидация формата, ограничение результатов)
- **Всего найдено багов:** 6

---

## ✅ Рекомендации по исправлению

### Приоритет 1 (Критический):
1. ✅ Добавить фильтрацию по пользователю в `get_user_statistics`
2. ✅ Исправить дублирование docstring в `create_country_ajax`

### Приоритет 2 (Высокий):
3. ✅ Добавить обработку null значений для `uploaded_at` в `get_factory_details`
4. ✅ Добавить валидацию длины полей в `create_country_ajax` и `create_factory_ajax`

### Приоритет 3 (Средний):
5. ✅ Добавить валидацию формата кода страны
6. ✅ Добавить ограничение на количество результатов в `get_factories` и `get_countries`

---

## 📝 Примечания

- BUG-69 критичен для безопасности - утечка статистики всех заказов
- BUG-70 - косметическая проблема, но может привести к путанице при чтении кода
- BUG-71 может привести к 500 ошибкам при работе с заказами без `uploaded_at`
- BUG-72 важен для предотвращения DoS атак через длинные строки
- BUG-73 важен для целостности данных и интеграций
- BUG-74 важен для производительности API


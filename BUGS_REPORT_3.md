# 🐛 Отчет о найденных багах (QA Analysis - Раунд 3)

**Дата анализа:** 2025-12-08  
**Аналитик:** QA Engineer  
**Методология:** Глубокий анализ Celery задач, email отправки и файловых операций

---

## 🔴 КРИТИЧЕСКИЕ БАГИ

### BUG-18: Отсутствует проверка email адреса в `send_notification_email`

**Файл:** `orders/tasks.py:20-147`

**Проблема:**
```python
user = notification.user
# ... нет проверки user.email ...
email = EmailMessage(
    to=[user.email],  # ⚠️ Если user.email пустой или None - будет ошибка!
)
email.send(fail_silently=False)
```

**Описание:**
Если у пользователя не указан email адрес (`user.email` пустой или `None`), задача упадет с ошибкой при попытке отправить email. Это приведет к:
- Неудачным retry попыткам
- Засорению логов ошибками
- Невозможности обработать уведомления для пользователей без email

**Воспроизведение:**
1. Создать пользователя без email адреса
2. Создать уведомление для этого пользователя
3. Запустить `send_notification_email.delay(notification_id)`
4. Получить ошибку при отправке email

**Исправление:**
Добавить проверку перед отправкой:
```python
if not user.email:
    return f"User {user.username} has no email address"
```

---

### BUG-19: Отсутствует проверка email адреса в `send_order_notification`

**Файл:** `orders/tasks.py:222-281`

**Проблема:**
```python
user = order.employee
# ... нет проверки user.email ...
notification = Notification.objects.create(...)
send_notification_email.delay(notification.id)  # ⚠️ Упадет если user.email пустой!
```

**Описание:**
Аналогично BUG-18, но в другой задаче. Создается уведомление, но email не может быть отправлен из-за отсутствия адреса.

**Исправление:**
Добавить проверку перед созданием уведомления или перед отправкой email.

---

### BUG-20: Отсутствует проверка email адреса в `check_overdue_orders`

**Файл:** `orders/tasks.py:150-219`

**Проблема:**
```python
for order in overdue_orders:
    # ... нет проверки order.employee.email ...
    notification = Notification.objects.create(...)
    send_notification_email.delay(notification.id)  # ⚠️ Упадет если email пустой!
```

**Описание:**
В цикле обработки просроченных заказов нет проверки наличия email у пользователя. Это приведет к множественным ошибкам при обработке заказов пользователей без email.

**Исправление:**
Добавить проверку перед созданием уведомления:
```python
if not order.employee.email:
    continue
```

---

### BUG-21: Потенциальная утечка памяти в `download_all_efactura_files`

**Файл:** `orders/views/efactura_views.py:280-287`

**Проблема:**
```python
with efactura_file.file.open('rb') as file_handle:
    file_content = file_handle.read()  # ⚠️ Читает ВЕСЬ файл в память!
    zip_file.writestr(filename, file_content)
```

**Описание:**
Все файлы в корзине читаются целиком в память перед добавлением в ZIP. Если в корзине много больших файлов (например, 10 файлов по 50MB каждый = 500MB в памяти), это может вызвать:
- Out of Memory (OOM) ошибки
- Медленную работу сервера
- Недоступность страницы при больших корзинах

**Воспроизведение:**
1. Создать корзину E-Factura
2. Загрузить 10+ файлов по 50MB каждый
3. Попытаться скачать все файлы
4. Получить OOM ошибку или очень медленную работу

**Исправление:**
Использовать порционное чтение файлов:
```python
chunk_size = 8192  # 8KB chunks
with efactura_file.file.open('rb') as file_handle:
    while True:
        chunk = file_handle.read(chunk_size)
        if not chunk:
            break
        zip_file.writestr(filename, chunk, compress_type=zipfile.ZIP_DEFLATED)
```

Или использовать `shutil.copyfileobj()` для копирования файлов в ZIP.

---

## 🟠 ВЫСОКИЙ ПРИОРИТЕТ

### BUG-22: Отсутствие retry механизма в некоторых Celery задачах

**Файл:** `orders/tasks.py:150, 222, 284, 375, 391, 436`

**Проблема:**
```python
@shared_task  # ⚠️ Нет bind=True, max_retries, default_retry_delay!
def check_overdue_orders():
    # При ошибке задача просто упадет без retry
```

**Описание:**
Несколько Celery задач не имеют retry механизма:
- `check_overdue_orders` - может упасть при проблемах с БД
- `send_order_notification` - может упасть при проблемах с БД
- `create_default_notification_templates` - может упасть при проблемах с БД
- И другие задачи без `bind=True`

**Исправление:**
Добавить retry механизм:
```python
@shared_task(bind=True, max_retries=3, default_retry_delay=60)
def check_overdue_orders(self):
    try:
        # ... код ...
    except Exception as e:
        if self.request.retries < self.max_retries:
            raise self.retry(countdown=60 * (2**self.request.retries))
        raise
```

---

### BUG-23: Race condition при создании NotificationSettings в `check_overdue_orders`

**Файл:** `orders/tasks.py:173-179`

**Проблема:**
```python
try:
    settings_obj = NotificationSettings.objects.get(user=order.employee)
except NotificationSettings.DoesNotExist:
    settings_obj = NotificationSettings.objects.create(user=order.employee)  # ⚠️ Может быть IntegrityError!
```

**Описание:**
Если два процесса одновременно обрабатывают заказы одного пользователя без настроек, оба попытаются создать `NotificationSettings`, что приведет к `IntegrityError` из-за `OneToOneField`.

**Исправление:**
Использовать `get_or_create()`:
```python
settings_obj, created = NotificationSettings.objects.get_or_create(
    user=order.employee
)
```

---

## 🟡 СРЕДНИЙ ПРИОРИТЕТ

### BUG-24: Отсутствует проверка на None для `order.employee` в задачах

**Файл:** `orders/tasks.py:227, 174`

**Проблема:**
```python
user = order.employee  # ⚠️ Может быть None!
# ... использование user без проверки ...
```

**Описание:**
Если `order.employee` равен `None` (хотя это маловероятно из-за ForeignKey), код упадет с `AttributeError`.

**Исправление:**
Добавить проверку:
```python
if not order.employee:
    return f"Order {order_id} has no employee assigned"
```

---

## 📊 Статистика Раунда 3

- **Критических багов:** 4
- **Высокий приоритет:** 2
- **Средний приоритет:** 1
- **Всего:** 7 багов

---

## ✅ Рекомендации по исправлению

1. **Добавить проверки email адресов** во всех задачах отправки email
2. **Исправить утечку памяти** в `download_all_efactura_files`
3. **Добавить retry механизм** во все Celery задачи
4. **Использовать `get_or_create()`** вместо `get()` + `create()` для предотвращения race conditions

---

## 📝 Общая статистика (Раунд 1 + Раунд 2 + Раунд 3)

- **Всего найдено багов:** 24
- **Критических:** 13
- **Высокий приоритет:** 5
- **Средний приоритет:** 5
- **Низкий приоритет:** 1


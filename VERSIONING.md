# 📌 Руководство по версионированию XXL OrderHub

## 🎯 Текущая версия: 1.0.0

## 📋 Формат версии

Проект использует **Semantic Versioning (SemVer)** формат: `MAJOR.MINOR.PATCH`

- **MAJOR** (1.x.x) - Критические изменения, несовместимые с предыдущими версиями
- **MINOR** (x.1.x) - Новые функции, обратно совместимые
- **PATCH** (x.x.1) - Исправления багов, обратно совместимые

## 🔄 Как изменить версию

### Способ 1: Вручную (простой)

1. Откройте файл `xxl_orderhub/settings.py`
2. Найдите строку:
   ```python
   VERSION = '1.0.0'
   ```
3. Измените на нужную версию, например:
   ```python
   VERSION = '1.1.0'  # для новой функции
   VERSION = '1.0.1'  # для исправления бага
   VERSION = '2.0.0'  # для критических изменений
   ```
4. Сохраните файл
5. Версия автоматически обновится на сайте после перезапуска

### Способ 2: Через скрипт (рекомендуется)

Используйте скрипт `update_version.sh`:

```bash
# Обновить PATCH версию (1.0.0 -> 1.0.1)
./update_version.sh patch

# Обновить MINOR версию (1.0.0 -> 1.1.0)
./update_version.sh minor

# Обновить MAJOR версию (1.0.0 -> 2.0.0)
./update_version.sh major

# Установить конкретную версию
./update_version.sh 1.2.3
```

## 📝 Когда менять версию

### PATCH (x.x.1) - Исправления багов
- Исправление критических багов
- Исправление безопасности
- Исправление производительности
- Исправление UI/UX проблем

**Примеры:**
- `1.0.0` → `1.0.1` - Исправлен баг с загрузкой файлов
- `1.0.1` → `1.0.2` - Исправлена утечка памяти

### MINOR (x.1.x) - Новые функции
- Добавление новых функций
- Добавление новых API endpoints
- Улучшение существующих функций
- Добавление новых шаблонов email

**Примеры:**
- `1.0.0` → `1.1.0` - Добавлена аналитика платежей
- `1.1.0` → `1.2.0` - Добавлена система уведомлений

### MAJOR (1.x.x) - Критические изменения
- Изменение API, несовместимое с предыдущими версиями
- Изменение структуры базы данных (миграции с breaking changes)
- Удаление функций
- Изменение формата данных

**Примеры:**
- `1.9.0` → `2.0.0` - Переработана система подтверждений
- `2.0.0` → `3.0.0` - Изменен формат хранения файлов

## 🚀 Процесс обновления версии

### 1. Перед релизом

```bash
# 1. Обновить версию
./update_version.sh minor  # или patch, major

# 2. Проверить изменения
git diff xxl_orderhub/settings.py

# 3. Закоммитить изменения
git add xxl_orderhub/settings.py
git commit -m "chore: Обновлена версия до 1.1.0"

# 4. Создать тег
git tag -a v1.1.0 -m "Release version 1.1.0"
git push origin main --tags
```

### 2. После релиза

Версия автоматически отобразится:
- ✅ В футере сайта
- ✅ В title страницы
- ✅ В navbar рядом с логотипом

## 📍 Где хранится версия

Версия хранится в одном месте:
- `xxl_orderhub/settings.py` - константа `VERSION`

И автоматически доступна через:
- `orders/context_processors.py` - context processor `app_version`
- Все шаблоны через переменную `{{ app_version }}`

## 🔍 Проверка текущей версии

```bash
# Через Python
python manage.py shell -c "from django.conf import settings; print(settings.VERSION)"

# Через grep
grep "VERSION = " xxl_orderhub/settings.py
```

## 📚 История версий

### 1.0.0 (Текущая)
- Первый стабильный релиз
- Полный функционал управления заказами
- Система подтверждений
- E-Factura интеграция
- Аналитика и отчеты

---

## 💡 Рекомендации

1. **Всегда обновляйте версию перед релизом** - не забывайте менять версию перед деплоем
2. **Используйте теги Git** - создавайте теги для каждой версии
3. **Ведите CHANGELOG** - документируйте изменения в каждой версии
4. **Следуйте SemVer** - используйте правильный формат версионирования

## 🛠 Автоматизация

Для автоматизации можно использовать:
- Git hooks (pre-commit для проверки версии)
- CI/CD pipeline (автоматическое создание тегов)
- Скрипты деплоя (автоматическое обновление версии)


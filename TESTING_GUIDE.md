# 🧪 Инструкция по тестированию функционала E-Factura для турецких фабрик

## 📋 Подготовка

1. **Убедитесь, что контейнеры запущены:**
   ```bash
   docker-compose ps
   ```

2. **Откройте приложение в браузере:**
   - URL: `http://localhost:8280` (или ваш production URL)
   - Войдите в систему

## 🎯 Тест 1: Проверка выбора типа фактуры для турецких фабрик

### Шаг 1: Найдите турецкую фабрику
1. Перейдите в **"Управление" → "Фабрики"**
2. Найдите фабрику со страной **"Турция"** (код страны должен быть **TR**)
3. Если турецких фабрик нет, создайте тестовую:
   - Перейдите в **"Управление" → "Страны"**
   - Убедитесь, что есть страна **"Турция"** с кодом **TR**
   - Если нет, создайте её
   - Создайте фабрику для Турции

### Шаг 2: Создайте заказ для турецкой фабрики
1. Перейдите в **"Заказы" → "Создать заказ"**
2. Выберите турецкую фабрику
3. Заполните остальные поля и создайте заказ

### Шаг 3: Проверьте наличие checkbox'ов для типа фактуры
1. Откройте созданный заказ
2. Отправьте заказ на фабрику (статус должен стать "Отправлен")
3. Загрузите инвойс (статус должен стать "Инвойс получен")
4. **Проверьте:** В правой панели "Действия" должны появиться два checkbox'а:
   - ☐ **Factura Export**
   - ☐ **E-Factura Turkey**
5. **Проверьте:** Должно быть предупреждение: "⚠️ Необходимо выбрать тип фактуры для завершения заказа"

## 🎯 Тест 2: Валидация выбора типа фактуры

### Шаг 1: Попытка завершить заказ без выбора типа фактуры
1. На заказе со статусом "Инвойс получен" (турецкая фабрика)
2. Нажмите кнопку **"✅ Завершить заказ"**
3. **Ожидаемый результат:** Должна появиться ошибка:
   - "Для турецких фабрик необходимо выбрать тип фактуры (Factura Export или E-Factura Turkey) перед завершением заказа!"

### Шаг 2: Выбор обоих типов одновременно
1. Отметьте оба checkbox'а (Factura Export и E-Factura Turkey)
2. **Ожидаемый результат:** Должна появиться ошибка:
   - "Можно выбрать только один тип фактуры!"

### Шаг 3: Корректный выбор одного типа
1. Отметьте только **"E-Factura Turkey"**
2. **Проверьте:** Предупреждение должно исчезнуть
3. Теперь можно завершить заказ

## 🎯 Тест 3: Автоматическое добавление в корзину E-Factura

### Шаг 1: Завершите заказ с выбранным E-Factura Turkey
1. Выберите **"E-Factura Turkey"**
2. Нажмите **"✅ Завершить заказ"**
3. Подтвердите завершение заказа
4. **Ожидаемый результат:** 
   - Заказ должен быть завершен
   - Файл инвойса должен автоматически попасть в корзину E-Factura для текущего месяца

### Шаг 2: Проверьте корзину E-Factura
1. Перейдите в **"Платежи" → "Корзины E-Factura"**
2. Найдите корзину для текущего месяца (например, "Декабрь 2025")
3. Откройте корзину
4. **Проверьте:** В списке файлов должен быть файл из завершенного заказа

## 🎯 Тест 4: Работа с корзинами E-Factura

### Шаг 1: Просмотр списка корзин
1. Перейдите в **"Платежи" → "Корзины E-Factura"**
2. **Проверьте:** Должен отображаться список корзин, отсортированный по году и месяцу (новые сверху)
3. **Проверьте:** Для каждой корзины отображается:
   - Название корзины
   - Год и месяц
   - Количество файлов
   - Кто создал и когда

### Шаг 2: Фильтрация корзин
1. Используйте фильтры по году и месяцу
2. **Проверьте:** Список должен фильтроваться корректно

### Шаг 3: Просмотр деталей корзины
1. Откройте любую корзину
2. **Проверьте:** Отображается:
   - Информация о корзине
   - Форма для загрузки нового файла
   - Список файлов в корзине

### Шаг 4: Загрузка файла в корзину
1. В детальном просмотре корзины заполните форму:
   - Выберите заказ (только турецкие с E-Factura Turkey)
   - Выберите файл (PDF, XML, XLSX, XLS)
   - Укажите дату загрузки
   - Добавьте комментарий (опционально)
2. Нажмите **"Загрузить файл"**
3. **Ожидаемый результат:** 
   - Файл должен быть загружен
   - Должно появиться сообщение об успехе
   - Файл должен появиться в списке

### Шаг 5: Скачивание файлов
1. **Скачивание одного файла:**
   - Нажмите кнопку **"📄 Скачать"** рядом с файлом
   - **Проверьте:** Файл должен скачаться

2. **Скачивание всех файлов:**
   - Нажмите кнопку **"📦 Скачать все файлы"** вверху страницы
   - **Проверьте:** Должен скачаться ZIP архив со всеми файлами корзины

## 🎯 Тест 5: Автоматическое распределение по месяцам

### Шаг 1: Создайте заказы с разными датами
1. Создайте несколько заказов для турецких фабрик
2. Завершите их с выбранным E-Factura Turkey в разные месяцы (или измените дату загрузки файла)
3. **Проверьте:** Файлы должны автоматически попадать в корзины соответствующих месяцев

### Шаг 2: Проверьте создание новых корзин
1. Загрузите файл с датой в новом месяце
2. **Проверьте:** Должна автоматически создаться новая корзина для этого месяца

## 🎯 Тест 6: Проверка для НЕ турецких фабрик

### Шаг 1: Создайте заказ для не турецкой фабрики
1. Создайте заказ для фабрики из другой страны (например, Италия, Польша)
2. Доведите заказ до статуса "Инвойс получен"
3. **Проверьте:** Checkbox'ы для выбора типа фактуры НЕ должны отображаться
4. **Проверьте:** Заказ можно завершить без выбора типа фактуры

## 🎯 Тест 7: Проверка в админ-панели Django (опционально)

1. Войдите в Django Admin
2. Перейдите в **Orders → Orders**
3. Откройте заказ турецкой фабрики
4. **Проверьте:** Должны быть видны поля:
   - `factura_export`
   - `e_factura_turkey`
5. Перейдите в **Orders → E-Factura baskets**
6. **Проверьте:** Видны все созданные корзины
7. Перейдите в **Orders → E-Factura files**
8. **Проверьте:** Видны все загруженные файлы

## 🐛 Возможные проблемы и решения

### Проблема: Checkbox'ы не отображаются
- **Решение:** Убедитесь, что фабрика имеет страну с кодом **TR**

### Проблема: Ошибка при завершении заказа
- **Решение:** Проверьте, что выбран хотя бы один тип фактуры (но не оба одновременно)

### Проблема: Файл не попадает в корзину
- **Решение:** Проверьте логи контейнера: `docker-compose logs web`
- Убедитесь, что у заказа есть `invoice_file`

### Проблема: Корзина не создается автоматически
- **Решение:** Проверьте, что дата загрузки указана корректно

## ✅ Чек-лист успешного тестирования

- [ ] Checkbox'ы отображаются только для турецких фабрик
- [ ] Валидация работает (нельзя завершить без выбора)
- [ ] Нельзя выбрать оба типа одновременно
- [ ] Заказ завершается с выбранным типом фактуры
- [ ] Файл автоматически попадает в корзину при выборе E-Factura Turkey
- [ ] Корзины создаются автоматически по месяцам
- [ ] Можно загружать файлы в корзины вручную
- [ ] Можно скачивать отдельные файлы
- [ ] Можно скачивать все файлы корзины в ZIP
- [ ] Для не турецких фабрик checkbox'ы не отображаются

## 📝 Примечания

- Все тесты должны выполняться на заказах со статусом **"Инвойс получен"**
- Для тестирования можно использовать тестовые данные или реальные заказы
- Рекомендуется тестировать в отдельной тестовой среде перед production


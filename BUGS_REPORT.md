# 🐛 Отчет о найденных багах (QA Analysis)

**Дата анализа:** 2025-12-08  
**Аналитик:** QA Engineer  
**Методология:** Глубокий анализ кода, проверка edge cases, race conditions, security issues

---

## 🔴 КРИТИЧЕСКИЕ БАГИ (Требуют немедленного исправления)

### BUG-1: Race Condition в `confirmation_approve` - неатомарная проверка статуса

**Файл:** `orders/views/confirmation_views.py:600-610`

**Проблема:**
```python
if confirmation.status != 'pending':
    messages.error(request, 'Это подтверждение уже обработано!')
    return redirect('confirmation_detail', pk=pk)

if confirmation.is_expired():
    # Автоматически помечаем как истекшее
    if confirmation.status == 'pending':  # ⚠️ СТАТУС МОЖЕТ ИЗМЕНИТЬСЯ МЕЖДУ ПРОВЕРКАМИ!
        confirmation.status = 'expired'
        confirmation.save(update_fields=['status'])
```

**Описание:**
Между проверкой `status != 'pending'` и `is_expired()` другой запрос может изменить статус confirmation. Это приводит к:
- Двойному выполнению операции
- Несогласованности данных
- Возможности обхода проверок

**Воспроизведение:**
1. Пользователь A открывает страницу подтверждения (status='pending')
2. Пользователь B подтверждает это же подтверждение (status='confirmed')
3. Пользователь A нажимает "Подтвердить"
4. Проверка `status != 'pending'` проходит (т.к. объект в памяти еще старый)
5. Операция выполняется повторно!

**Исправление:**
Использовать атомарное обновление через `confirm()` метод, который уже имеет правильную логику.

---

### BUG-2: Утечка данных - `ConfirmationListView` показывает ВСЕ подтверждения

**Файл:** `orders/views/confirmation_views.py:42-52`

**Проблема:**
```python
def get_queryset(self):
    """Get confirmations for orders belonging to the current user."""
    queryset = OrderConfirmation.objects.filter(
    ).select_related('order', 'requested_by', 'confirmed_by').order_by('-requested_at')
    # ⚠️ НЕТ ФИЛЬТРАЦИИ ПО ПОЛЬЗОВАТЕЛЮ!
```

**Описание:**
Любой пользователь может видеть ВСЕ подтверждения в системе, включая:
- Подтверждения других пользователей
- Конфиденциальную информацию о заказах
- Данные о действиях других сотрудников

**Воспроизведение:**
1. Войти как обычный пользователь
2. Перейти в `/confirmations/`
3. Увидеть все подтверждения всех пользователей

**Исправление:**
```python
def get_queryset(self):
    queryset = OrderConfirmation.objects.filter(
        Q(order__employee=self.request.user) | Q(requested_by=self.request.user)
    ).select_related('order', 'requested_by', 'confirmed_by').order_by('-requested_at')
```

---

### BUG-3: Race Condition в `Invoice.save()` - двойной пересчет `total_paid`

**Файл:** `orders/models.py:1009-1025`

**Проблема:**
```python
try:
    in_atomic = transaction.get_connection().in_atomic_block
except (AttributeError, Exception):
    in_atomic = False  # ⚠️ Может быть False даже если мы в транзакции!

if in_atomic:
    total_paid = self.payments.select_for_update().aggregate(...)
else:
    total_paid = self.payments.aggregate(...)  # ⚠️ БЕЗ БЛОКИРОВКИ!
```

**Описание:**
1. Проверка `in_atomic_block` может не работать корректно в некоторых случаях
2. Если проверка не сработала, пересчет происходит без блокировки
3. При одновременном создании платежей может произойти потеря данных

**Воспроизведение:**
1. Два пользователя одновременно добавляют платежи к одному инвойсу
2. Оба вызывают `Invoice.save()` вне транзакции (или проверка не сработала)
3. Оба пересчитывают `total_paid` без блокировки
4. Один из платежей теряется в расчете

**Исправление:**
Всегда использовать транзакцию при обновлении invoice через `InvoicePayment.save()`.

---

### BUG-4: Race Condition в `upload_efactura_file` - проверка дубликатов не атомарна

**Файл:** `orders/views/efactura_views.py:163-177`

**Проблема:**
```python
with transaction.atomic():
    existing_file = EFacturaFile.objects.filter(
        basket=basket,
        order=order
    ).first()  # ⚠️ НЕТ select_for_update()!
    
    if existing_file:
        return redirect(...)
    
    efactura_file = form.save(commit=False)
    efactura_file.save()  # ⚠️ Может быть создан дубликат между проверкой и сохранением!
```

**Описание:**
Между проверкой `existing_file` и `save()` другой запрос может создать файл для того же заказа. Это приведет к дубликатам.

**Воспроизведение:**
1. Пользователь A проверяет дубликаты (не найдено)
2. Пользователь B проверяет дубликаты (не найдено)
3. Пользователь A сохраняет файл
4. Пользователь B сохраняет файл
5. Два файла для одного заказа!

**Исправление:**
```python
existing_file = EFacturaFile.objects.select_for_update().filter(
    basket=basket,
    order=order
).first()
```

---

## 🟠 ВЫСОКИЙ ПРИОРИТЕТ

### BUG-5: Двойная проверка статуса в `confirmation_approve`

**Файл:** `orders/views/confirmation_views.py:600-632`

**Проблема:**
Проверка статуса происходит дважды:
1. В view: `if confirmation.status != 'pending'`
2. В методе `confirm()`: атомарная проверка

Это избыточно и может привести к путанице.

**Исправление:**
Убрать проверку из view, полагаться только на атомарную проверку в `confirm()`.

---

### BUG-6: Нет проверки статуса confirmation перед `confirm()` в `upload_invoice_execute`

**Файл:** `orders/views/confirmation_views.py:110-116`

**Проблема:**
```python
active_confirmation.confirm(
    request.user,
    comments=f'Инвойс {invoice.invoice_number} загружен...'
)
# ⚠️ Нет проверки, что confirmation еще pending!
```

**Описание:**
Если confirmation уже обработан, `confirm()` выбросит исключение, но это не обрабатывается должным образом.

---

## 🟡 СРЕДНИЙ ПРИОРИТЕТ

### BUG-7: Нет уникального ограничения на `EFacturaFile(order, basket)`

**Файл:** `orders/models.py:1529-1580`

**Проблема:**
В модели `EFacturaFile` нет `unique_together` для `(order, basket)`, что позволяет создавать дубликаты на уровне БД.

**Исправление:**
```python
class Meta:
    unique_together = [['order', 'basket']]
```

---

### BUG-8: Нет проверки статуса заказа в `_execute_complete_order`

**Файл:** `orders/views/confirmation_views.py:897-904`

**Проблема:**
После блокировки заказа нет проверки, что он еще не завершен. Если confirmation создан для уже завершенного заказа, операция все равно выполнится.

**Исправление:**
Добавить проверку после блокировки:
```python
if order.status == 'completed':
    raise ValueError('Заказ уже завершен!')
```

---

### BUG-9: Потенциальная проблема с `transaction.get_connection().in_atomic_block`

**Файл:** `orders/models.py:1010`

**Проблема:**
Проверка `in_atomic_block` может не работать корректно в некоторых версиях Django или при использовании разных бэкендов БД.

**Исправление:**
Использовать более надежный способ проверки или всегда использовать транзакции явно.

---

## 🟢 НИЗКИЙ ПРИОРИТЕТ

### BUG-10: Нет явной проверки CSRF токена в `confirmation_approve`

**Файл:** `orders/views/confirmation_views.py:612`

**Проблема:**
Хотя Django автоматически проверяет CSRF для POST запросов, было бы лучше добавить явную проверку для критических операций.

---

## 📊 Статистика

- **Критических багов:** 4
- **Высокий приоритет:** 2
- **Средний приоритет:** 3
- **Низкий приоритет:** 1
- **Всего:** 10 багов

---

## 🔧 Рекомендации

1. **Немедленно исправить:** BUG-1, BUG-2, BUG-3, BUG-4
2. **Добавить тесты:** Для всех race conditions
3. **Провести code review:** Особенно для критических операций
4. **Добавить мониторинг:** Для отслеживания дубликатов и несогласованности данных

---

## ✅ Положительные моменты

- Хорошее использование транзакций в большинстве мест
- Правильная обработка ошибок
- Использование `select_for_update()` в критических местах
- Атомарные обновления в методах `confirm()` и `reject()`

